version: 2

project_name: opentofu-kms-ovhcloud

before:
  hooks:
    - go mod tidy

builds:
  - id: opentofu-kms-ovhcloud
    main: ./cmd/opentofu-kms-ovhcloud
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    binary: "{{ .ProjectName }}-{{ .Os }}-{{ .Arch }}-{{ .Version }}"

archives:
  - formats: [ 'tar.gz' ]
    format_overrides:
      - goos: windows
        formats: [ 'zip' ]

checksum:
  name_template: "{{ .ProjectName }}_checksums.txt"

release:
  github:
    owner: ovh
    name: opentofu-kms-ovhcloud

  extra_files:
    - glob: install.sh

  footer: |

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser)
